# Local (rodando o backend fora do Docker ou via docker-compose)
VITE_API_URL=http://localhost:3000/api

# Produção — troca pela URL real do backend depois do deploy no Render
# VITE_API_URL=https://bancadaviva-backend.onrender.com/api
